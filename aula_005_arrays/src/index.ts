let notas : number[] = []; //array do tipo number
let paises : Array<string>;

let cidades = [
    'São Paulo',
    'Minas Gerais',
    'Divinópolis'
];


notas.push(10);

console.log(notas[0]);

console.log(cidades.length);